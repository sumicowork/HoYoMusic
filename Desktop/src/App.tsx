// App entry referenced by the router as `AppShell`. The real shell lives in
// src/components/layout/AppShell.tsx; re-export it here so createBrowserRouter's
// `import AppShell from '@/App'` keeps resolving.
export { default } from '@/components/layout/AppShell';
