// Tailwind CSS v4 is processed via the @tailwindcss/vite plugin (see vite.config.ts).
// This PostCSS config is kept minimal/compatible; it does not double-process styles.
export default {
  plugins: {},
};
