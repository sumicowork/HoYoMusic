# 2026-07-11 工作日志

## 并行 subagent 批量任务 (#38–#48) 核验 + 修复

### 核验结果（全部基于源码/产物，非文档）
- backend: `tsc --noEmit` 0 错；`vitest run` **34 passed**（5 文件：cache/contract/drift/gameController/schemas）。
- frontend: `tsc --noEmit` 0 错；`vitest run` **19 passed**（format/authModalStore/ThemeToggle）。
- desktop: `tsc --noEmit` 0 错；`vitest run` **37 passed**（smoke/cn/playerStore/tauri/useAudioPlayer）。
- Rust: `cargo check` 0 错（reqwest 真实下载已实现并编译）。
- Tauri 打包成功：产出 `desktop/src-tauri/target/release/hoyomusic-desktop.exe`。

### 我亲自修掉的 4 个 desktop 测试失败（两个是真 bug）
1. `playNext` / `playPrev` 缺 `repeat==='one'` 分支 → 单曲循环时仍切歌。已补（replay 当前曲）。`cycleRepeat` 会产出 'one'，必须处理。
2. `useAudioPlayer` 挂载时双 `play()`（[track] 与 [isPlaying] 两个 effect 同时触发）。重构为单一合并 effect，去重。
3. `setCurrent` 对新曲原为 `queue:[track]`（替换），测试期望 append。grep 确认 `setCurrent` 无任何生产代码调用（仅测试用），改为 append 零风险。

### 依赖修复
- frontend 缺装 `@testing-library/dom`（声明在 package.json 但未进 node_modules）→ `npm install --legacy-peer-deps`（react-beautiful-dnd@13 只认 react≤18，与项目 react19 冲突属既有 ERESOLVE，与本次无关）。

### 其他 agent 产出（已核验合理）
- 迁移系统落地：`backend/db/migrations/0001_init.sql` + `backend/scripts/migrate.ts`（pg 驱动，_migrations 表追踪，按序事务执行）→ 根治 schema 漂移根因。
- `backend/src/middleware/debugAuth.ts` 删除未用 `getRealIp`（死代码清理）。
- `docs/ARCHITECTURE_FROM_SOURCE.md` 新建（从源码反推）；其第201行提到旧 client-desktop/WinUI3 已删除，属准确陈述，非残留。

### 当前状态
四端（backend/frontend/desktop/rust）全部 tsc/vitest/cargo 绿，桌面端可打包 exe。

### 已提交大 checkpoint
- 用户确认「可以」提交。commit `ff28530`（chore: 统一收口多端重构大 checkpoint（2026-07-11）），420 files changed, +68986 / -27901。
- 内容涵盖：schema.sql 重建 + 迁移系统、client-desktop(C#/WinUI3) 删除、Tauri 桌面端(Desktop/，Windows 大小写不敏感，与 desktop/ 同目录)、三端测试、swagger 注解、admin 密码环境变量化、死代码清理、openapi 生成客户端、ARCHITECTURE_FROM_SOURCE.md。
- 注意：commit 一并纳入了 subagent 产生的 `.agents/skills/`（frontend-design/impeccable/ui-ux-pro-max/web-design-guidelines）与根 `package.json`/`package-lock.json`/`skills-lock.json`，均属本次并行 checkpoint 范围，用户已批准整包提交。
- 提交后 `git status` 干净，无残留。target/ 与 node_modules/ 已被 .gitignore 正确忽略（exe 不入库）。

## 桌面端 API 对齐修复（用户指出 api.ts 是我自己上一轮写歪的）

### 根因（基于源码核实，非文档）
`Desktop/src/lib/api.ts` 调用路径与后端真实挂载对不上：
- 列表接口漏写 `/public` 前缀（后端公开接口全在 `/api/public/*`）；自创了后端根本不存在的 `/home`、`/search`、`/albums/:id/tracks`、`/artists/:id/{albums,tracks}` 子路由。
- `fetchAlbum`/`fetchArtist` 把后端返回的 bundle（{album,tracks} / {artist,tracks,albums}）当单个对象用。
- shared.mapRawTrack 读 `artist_name` 而非真实 `artists[]`+`album_cover`。
- 打包 exe 断连：`.env.example` 里 `VITE_API_BASE=/api`（相对路径在 Tauri WebView 里解析到 tauri://localhost 必死）。

### 后端真实契约（已逐一读控制器/路由确认）
- 公开（无需鉴权）：`/api/public/tracks?search/page/limit`、`/api/public/tracks/:id`、`/api/public/tracks/:id/stream`、`/api/public/tracks/random`、`/api/public/top-tracks`、`/api/public/albums/random`、`/api/public/covers/proxy`。
- `/api/albums`、`/api/albums/:id` GET 无鉴权中间件（仅 POST/PUT/cover 需 admin）。
- `/api/artists`、`/api/artists/:id` GET 无鉴权（仅 POST/PUT 需 admin）；`getArtistById` 用 `req.params.id` 当**艺术家名**查（artist.id 为 null）。
- `/api/playlists`、`/api/playlists/:id` 需 JWT → 桌面端无登录，**优雅降级为空**（首页/库/歌单页显示"暂无"，不崩溃）。
- 返回均包 `{success,data}`；artist 列表行只有 `name`（无 id）、album 详情返回 `{album,tracks,discs}`、artist 详情返回 `{artist,tracks,albums,games}`。

### 改动文件（均 tsc 0 / vitest 37 passed）
- `Desktop/src/lib/api.ts`：重写——BASE 默认绝对 `http://localhost:3000/api`（dev+exe 都直连，CORS 默认全开），mapper 容错（artists 数组/字符串、album_cover、audio_url 兜底 stream）；fetchTracks/searchTracks→/public/tracks、fetchTrack→/public/tracks/:id、fetchAlbums→/api/albums、fetchAlbum→/api/albums/:id 返回 {album,tracks}、fetchArtists→/api/artists、fetchArtist→/api/artists/:id 返回 {artist,tracks,albums}、fetchHome 用 random/top-tracks 拼装、playlists 返回空。
- `Desktop/src/pages/Album.tsx`、`Artist.tsx`：改用新 bundle 返回，删除各自错误的 mapRawTrack/mapRawAlbum。
- `Desktop/src/pages/Library.tsx`：艺术家链接加 `encodeURIComponent`（后端对称 decode）。
- `Desktop/.env.example`：`VITE_API_BASE` 默认改绝对地址，并注明 exe 不能用相对 `/api`。

### 剩余已知限制（非 bug，需登录/系统能力）
- 歌单功能需 JWT（桌面端未做登录）→ 显示空。
- 系统媒体键（SMTC）= Rust 桩，仅应用内按钮+全局快捷键有效。
- 需在本地先起后端（默认 :3000）才能看到真实数据；`tauri dev` 与打包 exe 现均能连。

## 提交桌面端修复并实际启动运行（用户要求"提交并运行看 UI"）
- 提交 `330484c`（fix(desktop): 对齐后端真实公开接口契约并修复打包后断连），5 files +181/−145。仅桌面端源码，未带 .workbuddy 内部记忆。
- 实际启动成功：
  - 后端 `npm run dev`（nodemon+ts-node）后台运行于 :3000，已验证 `/api/public/albums/random` 等返回 200 且含真实数据（tracks/albums/artists 均有内容；top-tracks 空因 play_count 全 0，属真实数据行为）。
  - `npm run tauri dev` 首次全量编译 Rust 依赖（420 crate，约 1m37s）→ 弹出原生窗口 `hoyomusic-desktop.exe`（PID 39192），Vite 开发服务器在 :5173 监听。窗口进程与后端均存活，UI 已可交互、连真实曲库。
- **运行坑（已踩并已解）**：`tauri` CLI 内部调用 `cargo`，但 Bash 工具默认 PATH 不含 `~/.cargo/bin`（`cargo check` 之前是用绝对路径 `/c/Users/sumi/.cargo/bin/cargo` 跑的，所以没暴露）。`tauri dev` 直接报 "program not found: cargo"。修复：`export PATH="/c/Users/sumi/.cargo/bin:$PATH"` 后重跑即可。下次启动桌面端必须先带此 PATH。
- 日志：backend.log、desktop-dev.log 在仓库根。
- top-tracks 为空属正常（无播放量数据），不影响其余板块。

## 桌面端改为亮色主题（用户要求"配色请做亮色主题"）
- 原本是**暗色专用**：AntD `darkAlgorithm` + `color-scheme: dark` + 两套重复 `:root` CSS 变量（AppShell 内联 `shellCss` 与 `global.css`）+ `theme.ts` 暗色 token + `ThemeMode='dark'`。
- 改亮色（保留 HoYo 紫 #7F77DD / 青 #37C6D9 强调色，底色转浅）：
  - `global.css` 与 `AppShell.tsx` 的 `:root` token → 浅色（--background-base #f4f4f7 / --background-base-alt #fff / --surface #fff / --surface-hover #ececf2 / --text-primary #1a1a22 / --text-secondary #6b6b78 / --border rgba(0,0,0,0.08)），`color-scheme: light`；滚动条/骨架屏 hover 用 `rgba(0,0,0,*)` 替代白色；body 径向辉光保留淡紫。
  - `AppShell.tsx` 的 AntD `ConfigProvider`：`algorithm: antdTheme.defaultAlgorithm`，`colorBgBase #f4f4f7` / `colorTextBase #1a1a22`。
  - `theme.ts` token 同步浅色，`ThemeMode='light'`，注释去掉 "dark-only"。
  - `ProgressBar.tsx` 拖动圆点 `bg-white` → `bg-[var(--accent)]`（亮色轨道上白点看不见）。
- 全桌面源码 grep 暗色硬写十六进制值 = 0 处，UI 组件本就 token 驱动。tsc 0 错。
- 重启 `tauri dev`（依赖已缓存，重编 ~12s）后窗口稳定存活（PID 78524，vite 5173=200），亮色界面已弹出。
- **注意行为**：`tauri dev` 在**窗口被关闭时会跟随退出**（整个 dev 进程树终止，vite 也停）。所以窗口被关=开发服务器停。要再看就让我重启用 `export PATH=...cargo/bin:$PATH && npm run tauri dev`。未提交（待用户确认）。

## 产品/架构/数据库综合分析（用户要求"先读库、分析是什么网站、设计模块、DB 可能也不合理"）

### 数据导入问题（用户问"是否导入了下载的数据包"）
- **结论：没有导入。** 本地 `hoyomusic` 库仅有极小种子数据（tracks=4/albums=4/games=7/users=1/tag_groups=6/visit_logs=32，其余 0 行）。之前只 `pg_dump` 拉了线上库 **schema** 重建 `backend/db/schema.sql`（27 表定义，本地实装 26 表，缺 `artist_avatars`）。
- 磁盘（Downloads/项目/.workbuddy 备份）**找不到任何下载的数据包文件**。本地另有一个 `hoyodb` 库是**另一个 Prisma 项目**（materials/tags/download_logs），与音乐无关。
- 若用户确有数据包需导入：属破坏性（覆盖现有数据），须按安全规则先备份+用户确认。已明确告知。

### 产品定位
HoYoMusic = 面向 HoYoverse 游戏（原神/星铁/绝区零/崩坏3/未定事件簿/因缘精灵/星布谷地）的**同人音乐曲库 + 在线试听 + 资料整理**站点，含无损(FLAC 48k/24bit)播放下载、用户体系、管理后台(含元数据批量导入可回滚)、站内信/反馈、合规(维护/备案)、访问/播放统计。

### DB 结构批判（综合）
- 优点：28 个外键齐全、删除语义合理；uuid 对外引用；索引到位。
- **P0 重大缺陷**：① 艺术家无一级实体表（靠 `track_credits` EAV 推导，`artist_avatars` 以名字字符串为键，改名即断链，`artist.id` 为 null）→ 应新增 `artists` 表 + `track_credits.artist_id` 外键；② 两套分类体系（`music_source_*` 按游戏三级树 + `tags`/`tag_groups` 全局层级）职责重叠命名晦涩 → 显式分工并改名（曲源树 vs 编辑标签）；③ `tracks`/`albums` 标题三列(title/title_cn/title_en)冗余 → 改 title(展示)+title_original(原名)。
- **P1**：`track_credits` EAV 无法类型化查艺术家；`users` 仅 `is_admin` 无 `role`(用户查 `role` 列报错)；`visit_logs`/`track_play_events` 宽表无保留策略会膨胀；`music_source_nodes.game_id` 可由 category 推导(冗余)。
- **P2**：app_settings 混多关注；album_discs+tracks.disc_id 三段式定位；本地缺 artist_avatars 表(漂移)。
- 所有改动须走迁移、对线上真值库向后兼容。

### 前端/桌面端 IA 混乱（源码探查结论）
- 路由命名两端矛盾：FE `/albums` `/artists` `/playlists` vs Desktop `/album` `/artist` `/playlist`；`/library` FE 叫"曲库"/Desktop 叫"乐库"。
- 同概念三套文案：曲库/乐库、主页/首页、艺术家/创作者。
- 孤儿页：FE `/tags` `/tags/:id` `/games/:id` 菜单无入口；Desktop `/album` `/artist` 不在侧栏。
- Home vs 曲库职责重叠；标签公开端无入口却后台有 TagManagement。
- 后台语义乱：`/admin` label 写"曲目管理"实为仪表盘；退出 `navigate('/admin/login')` 是无效路由；"Music Source 库" 英文 label；后台入口对全体用户可见才被拦。Desktop 无账号体系/无 admin/无维护模式。

### 统一方案（已写入报告）
- 抽 `routes.ts` 共享路由常量（集合资源用复数）；定中文术语表（曲库/艺术家/歌单/首页…）；补导航入口(标签/游戏)、修 Home vs 曲库边界、修后台术语与退出路由、仅管理员见后台入口、桌面端精简为播放器导航。
- 产出报告：`docs/architecture-review-2026-07-11.md`（含模块表、DB 批判、IA 混乱清单、统一方案、实施顺序）。本轮仅分析未改代码/数据。建议下一步从"统一术语表+路由常量"和"修前端菜单/孤儿页"入手（纯前端低风险）。

## 真正的数据包出现了 + 还原到并行库（用户给路径）
- 用户指向 `C:/Users/sumi/Downloads/hoyomusic_20260710221108aaaem.sql.gz`（此前我的磁盘搜索漏了 `.sql.gz`）。
- **格式澄清**：虽然后缀 `.sql.gz`，其实是 **`pg_dump -Fc` 自定义二进制格式 + gzip**，不是纯文本 SQL → 必须用 `pg_restore`，不能 `psql`。`pg_restore` 这版不接受 `-` stdin，且原生 Windows 程序不认 GitBash 的 `/tmp` → 解压到 `C:/.../db/backups/hoyo_import.dump`（Windows 绝对路径）再 `pg_restore -U postgres -O -d hoyomusic_import`。`--no-owner` 跳过服务器角色 `hoyomusic_user`。
- **安全做法**：先 `pg_dump -Fc` 备份现有种子库到 `db/backups/hoyomusic_seed_preimport_20260711.dump`（107K），再还原到**全新并行库 `hoyomusic_import`**，原 `hoyomusic`（4 首种子）完全没动（canary：导入后 `hoyomusic.tracks` 仍=4）。
- **真实规模**：tracks **3594** / albums 146 / album_discs 65 / games 8 / users 15 / track_credits **26125** / tags 100 / tag_groups 3 / music_source_categories 12 / music_source_nodes 2550 / track_tags 3594 / track_music_sources 3988 / favorites 31 / playlists 4 / track_play_events 3937 / visit_logs **102394** / catalog_metadata_import_changes 2830 等。
- **数据推翻了此前一项推测**：`users` **根本没有 `role` 列**（之前架构报告写"只有 is_admin 无 role"，实际是连 role 枚举都没有，查 `role` 直接报"字段不存在"）。权限纯二元 is_admin。
- **确认**：无 `artists` 表；艺术家靠 `track_credits.credit_value`(自由字符串)+`artist_aliases`(9)/`artist_role_aliases`(39) 归一辅助（无 FK 强制）。分类三套并存：tags(描述性分面) / music_source(游戏内出处树) / album-game(结构归属)。app_settings 含真实 ICP(鲁ICP备2026012072号-1)+公安备案号→确系公网站点、内测阶段。
- 产出数据驱动报告：`docs/db-content-analysis-2026-07-11.md`（真实行数画像、模块设计、DB 批判 P0/P1/P2、前端/桌面端 IA 统一、下一步 (a)指后端到 hoyomusic_import / (b)覆盖 hoyomusic 二选一）。**待用户决定**怎么让真实数据跑起来（未擅自改 backend/.env）。

## 前端/桌面端 IA + 操作逻辑混乱分析（用户质疑"你是不是没分析"）
- 用户指出上一版把 UI 混乱只当 5 bullet 带过、太敷衍。已补**专门报告** `docs/ui-ia-chaos-analysis-2026-07-11.md`（落到 file:line，未改代码）。
- **前端三套打架导航**：`SideNav.tsx:36-62`(艺术家/管理全员可见) vs `PageHeader.tsx:83-91`(创作者/管理仅admin) vs `MobileTabBar.tsx:31-38`(创作者)。标签分歧：曲库 vs 乐库、艺术家 vs 创作者。
- **孤儿/死链**：前端 `/playlists/:id` 无菜单入口；`/admin/login` 死链(`AdminLayout.tsx:37`)；`AlbumDetail` MoreOutlined 无 onClick(`179-184`)。`/admin` 标"曲目管理"实为曲目表非仪表盘。
- **桌面端**：路由单数(/album /artist /playlist) vs 前端复数；侧栏仅 首页/乐库/搜索/歌单，`/album`/`/artist` 孤儿；`/artist`·`/playlist` 无 id 无限转圈；无登录→歌单全死("歌单加载失败")；点击即 setQueue 替换队列(setCurrent append 死代码)；下载 Rust 桥已实现但 UI 未接；媒体动作(next/prev)桩；503 静默空列表无维护页。
- **统一方案**：术语表(曲库/艺术家 为规范名，废 乐库/创作者)+ 复数路由 + 三导航合并一套 + 补队列操作/下载UI/维护页/登录降级 + DB 补 artists 实体(id 化)。优先级 P0(术语/死链/死按钮/无限转圈,纯文案低风险) → P1(结构) → P2(需DB重构)。**待用户授权落地**，建议最小起点 P0 1–5。

## 前端逐页精细 UX 审计（用户要求"详细分析每页看到什么/层级/跳转/合理性"，桌面端暂搁置）
- 用户明确：**先放下桌面端，专注前端精细 UI/UX 优化**。本轮只审前端，未改代码。
- 一手验证（非子代理二手）：读 `App.tsx` 路由树、`PageHeader.tsx`/`MobileTabBar.tsx`/`SideNav.tsx`/`AdminLayout.tsx`/`GameDetail.tsx`/`AlbumDetail.tsx`。
- **关键一手结论**：
  - 路由树扁平：公开路由无 `/games` 列表、`/games/:id` 与 `/track/:id` 是**孤儿详情**（无父列表路由）。
  - `GameDetail.tsx` **无 Hero**（只渲专辑网格）、未找到态英文 `Back to Home`/`Game not found`(78-80)、`getGameClass()` 硬编码游戏名(86-97)→新游戏无样式。**最弱页**。
  - `AlbumDetail.tsx:179-184` `MoreOutlined` 按钮**无 onClick = 死控件**。
  - `MobileTabBar.tsx:31-38` **只有 6 项，缺 `/tags` 和 `/me` 入口**；文案称"创作者"。
  - `SideNav.tsx` grep 全仓**仅自身引用 = 死代码**（真实导航是 PageHeader+MobileTabBar）。
  - `AdminLayout.tsx:37` 退出 `navigate('/admin/login')` 该路由不存在→落入 `*` 回首页 = bug。
  - `PageHeader.tsx:83-91` 真实桌面导航用"创作者"非"艺术家"。
- 合理性分级：✅ AlbumDetail/TrackDetail 沉浸式优秀、Search 高级筛选完整、TagDetail 面包屑清晰；⚠ 首页 5 区块杂、Playlists 简陋、未登录按钮消失无引导；✗ GameDetail 最弱。
- 产出：`docs/frontend-ux-audit-2026-07-11.md`（21 页逐页档案卡 + 跳转全局表 + 系统性问题 P0/P1/P2 + 层级图）。**待用户授权从 P0 哪条动手**（建议先抽跨端 `terminology.ts`/路由常量钉死术语，再修 P0 死链/死控件/移动端入口）。

## 前端各页 × 真实数据库 对照（用户要求"结合 DB 看每页设置是否合理"）
- 数据基准 `hoyomusic_import`（3594 首真实）。**关键发现（纠正此前纯代码审计 3 处误判）**：
  - **歌词表根本不存在**（information_schema 无 `%lyric%`）→ TrackDetail 歌词块永远空（前报误判为亮点）。
  - **tag_groups 仅 2 组**：`BPM`(97)+`人声类型`(3)，**无"游戏/主题曲/场景"层级** → TagDetail 面包屑是空壳（前报误判"清晰"）；标签维度偏离用户找歌需求。
  - **`play_count` 3594 行全 NULL** → 首页"热门 Top10"与按播放量排序=空转；analytics 播放维度失真。
  - **tracks 无 `game_id`**（经 album→game 间接关联；146/146 专辑有 game_id，覆盖原神42/绝区零29/崩坏3 27/星铁26/未定13/崩坏学园2 9）。
  - **8 游戏仅 6 有专辑**：星布谷地(id7)/崩坏因缘精灵(id6) 均 `status=unreleased` 且 0 专辑→GameDetail 空页；games 表有 description/cover_path 但 GameDetail 未显示（浪费数据）。
  - 填充率：tracks 规格100%、credits 87%、music_sources 42%、track_tags 100%。
- 合理性结论：🔴空转功能(热门/歌词/标签面包屑)；🔴GameDetail 最弱(浪费数据+空页+英文)；🟡标签维度错配；🟢曲库/专辑/专辑详情/个人中心稳。
- 产出：`docs/frontend-pages-vs-db-2026-07-11.md`（逐页×DB对照+健康度图）。**待用户决定是否据此调整重设计**（如首页热门改"最新/编辑精选"、游戏详情做Hero+歌曲直览、标签体系重构、歌词缺口处理）。

## 目的驱动 IA 对齐（用户确认四问答案，2026-07-11 晚）
- 用户明确平台**目的**：HoYoVerse 游戏音乐「资料/百科全书 + 无损试听」站，核心用户=**HOYO-MiX 粉丝**，来不只是听歌、更要了解每首曲子的丰富信息（在哪播/谁做的/类型风格语言）。对标 VGMdb/Discogs/OST wiki，非 QQ音乐。
- 四问答案：Q1 场景定位=**一等导航维度**(按游戏场景/任务树浏览)；Q2 创作者=**目的本身**(需独立可逛页, 按角色筛选+详情)；Q3 核心用户=HOYO-MiX 粉丝(听+了解信息)；Q4 游戏=**主入口**(用户自认不太确定，我判断正确：场景树天然按游戏分、是独特浏览的根)；曲库=用户质疑是否必要→**建议降级/合并进搜索**(无查询态=全部曲目+分面)；标签=描述性分面(类型/风格/是否人声/语言, 可点筛选)。
- **核心逻辑**：曲子(Track)是所有入口的终点；歌曲详情页是信息最丰富页(听+在哪播[场景面包屑可点]+完整credits+标签+所属专辑)。
- **两个依赖(已向用户说明)**：① 创作者要可点跳转需先有 `artists` 实体(归一 track_credits 自由字符串)——此前 P0 DB 改造；纯前端可先做"按名字聚合只读页"顶着。② 标签用户想要的"类型/风格/语言"目前库里没有(`tags` 表仅 BPM 97+人声类型 3)→维度很薄，需后续补打标签数据或先用现有。
- 已产出**目标 IA 提案（未改代码）**，待用户确认后逐页对比现状出改造方案。

## IA 落地：阶段一(地基)+阶段二(打通) 已实现并提交（2026-07-11 深夜）
- 用户指令「先落实 IA 问题」+「你全权决定顺序」。分三次提交策略（用户选）。
- **阶段一（地基，commit 9f14377）**：
  - 删「曲库」主导航（PageHeader + MobileTabBar）；曲库全曲浏览交给搜索无查询态（曲库 = 彻底删除是用户拍板）。
  - MobileTabBar 补「我的」(带登录态)、保留「反馈」(移动端 header 导航 ≤768px 隐藏, 反馈仅底栏有)，底栏 6 项。
  - 全站术语统一「艺术家→创作者」(公开页+后台+组件, replace_all)；`generated/api-types.ts` swagger 生成物保留不动。
  - 删死代码 SideNav.tsx/.css(全仓无引用)；修 AdminLayout 退出跳不存在的 /admin/login → /；删 AlbumDetail 未绑事件 MoreOutlined 死控件。
- **阶段二（打通，commit 3ac60ac）**：让 TrackDetail 成信息终点，关联信息全可点。
  - 关键数据事实：`Track` 类型**无 game 字段**，曲目↔游戏关联**只来自音乐来源**(`TrackMusicSourceItem.game_id`)。
  - 改 TrackDetail 标签 static `<Tag>` → `<Link to=/tags/:id>`。
  - 改 MusicSourcesDisplay 分组保留 game_id，游戏名 Tag → onClick navigate(/games/:id)。
  - 已存在的可点(本次未动)：credits 人名→/artists/:name(CreditsDisplay 早有)、专辑→/albums/:id、ArtistDetail 已是可逛创作者页(按名路由+参与游戏可点)。
  - 场景路径节点(music_source 树)跳转**仍留阶段三**——需先建场景浏览页。
- 顺带：Desktop 亮色主题(commit 9f0ddc1)、docs 5 报告+gitignore 排除 db/backups+*.dump(commit e903048) 一并提交。
- 验证：frontend tsc 0 + vitest 19✓。
- **阶段三待办**：① GameDetail 接场景树(music_source 2550 节点做树状浏览) + 场景节点可点；② 创作者要真去重跳转需后端 `artists` 实体表(现为 track_credits 自由字符串, ArtistDetail 靠按名聚合顶着)；③ 标签维度太薄(库里仅 BPM+人声类型 3)，用户想要的"类型/风格/语言"需补打标数据。

## IA 落地：阶段三-① 场景树浏览 已实现（2026-07-11 夜，用户授权「你独立判断即可」）
- **后端（纯只读，零迁移）**：`musicSourceController.ts` 新增两个公开函数，`publicRoutes.ts` 挂载：
  - `GET /api/public/games/:gameId/music-tree` → `getPublicGameMusicTree`：返回 `{game, categories[]}`；每分类含 `total_track_count` + 嵌套节点树；每节点带 `direct_track_count`(直挂) + `total_track_count`(含子孙，JS 迭代后序汇总，避免深树递归)。JS 建树 + 单次 group-by 直挂计数。
  - `GET /api/public/music-sources/nodes/:nodeId/tracks?page&limit` → `getPublicNodeTracks`：递归 CTE 取该节点+全部子孙的曲目，形状**严格对齐 getTracks**(t.* + album 字段 + artists json_agg + favorite_count)，带 node.path 面包屑 + 分页。
- **前端**：
  - `musicSourceService.ts` 加 `getGameMusicTree` / `getNodeTracks` + 类型 `PublicGameMusicTree`/`PublicMusicTreeNode`/`PublicNodeTracksResult`。
  - `GameDetail.tsx` **重写**：加游戏 Hero(名/英文名/描述/专辑数, 深色游戏背景上白字) + 「专辑/场景音乐」双 Tab(AntD Tabs)。场景 Tab = 左 AntD Tree(分类→节点, height 虚拟滚动, 节点标题带曲目数, 0 曲目淡显) + 右曲目 Table(标题/专辑/时长/播放·下载, 面包屑 node.path, 播放全部)。支持深链 `?tab=sources&node=<id>` 自动展开祖先+选中+加载。
  - `MusicSourcesDisplay.tsx`(TrackDetail 音乐来源块)：每条 path row 现可点 → `/games/{gameId}?tab=sources&node={node_id}`（**补齐阶段二暂缓的"场景路径可点跳转"**）。闭环：曲目→在哪播→游戏场景树。
- 验证：backend tsc 0 + vitest 34✓；frontend tsc 0 + vitest 19✓。端点实机 curl `success:true`。
- ⚠️**关键数据发现**：**后端连接的本地 `hoyomusic` 库音源三表全为 0 行**(categories/nodes/track_music_sources=0/0/0)；2550 节点/3988 关联数据**只在 `hoyomusic_import` 库**(线上 dump 还原库)。故 dev 环境场景树端点结构正确但**无数据可显**。需用户决定：把 backend/.env `DB_NAME` 指向 `hoyomusic_import`(一行改, 非破坏, 可逆) 或将音源数据导入 `hoyomusic`(破坏性, 需备份+确认)。**未擅自改**。
- 阶段三-② 创作者实体表 / ③ 补标签数据 仍未做。

## dev 后端 .env 指向 hoyomusic_import + 场景树实机验证（2026-07-11 夜，用户答"本地调试链接 import 即可"）
- **改动**：`backend/.env` `DB_NAME=hoyomusic` → `hoyomusic_import`（.env 已被 gitignore，不入库）。重启后端（旧进程 PID 48880 kill，`npm run dev` 重启，nodemon 不监听 .env 故必须手动重启）。**原 hoyomusic 库未动**。
- **实机验证（真实数据）**：`/public/games/1/music-tree` → 原神 12 分类/3988 关联（音源数据**全集中在原神 id=1**，其余 7 游戏为 0）；分类如 场景音乐(1490)/尘歌壶(215)/特殊玩法(38)/活动(204)。`/public/music-sources/nodes/2555/tracks` → 面包屑「枫丹>枫丹廷区>海薇玛映影欢乐城(白天)」、2 首、artists 数组/album/分页齐全。
- **计数语义坑（已想清并处理）**：树 `total_track_count`=子树各节点 COUNT(DISTINCT track_id) 之和=**出现次数**(同曲挂多子节点会重复累加，如 枫丹=269)；节点端点 `pagination.total`=整树 COUNT(DISTINCT t.id)=**distinct 曲目数**(枫丹=141)。二者不同属预期。前端：树上数字保留为"内容量提示"(不标"首")，面板计数用 pagination.total 标"共 N 首"。
- **分页修正(commit d53434b)**：后端 limit 上限 100，父节点>100 首单页截断 → 前端 Table 改**服务端受控分页**(每页50, onChange 重新拉)；播放全部跨页拉全后整队播放。
- 提交：`d0c21b1`(feat 场景树) + `d53434b`(fix 分页)。dev 环境 backend :3000(连 import 库) + frontend vite :5173 均在跑，已浏览器预览 `/games/1?tab=sources`。
