# 2026-07-10

## 项目现状分析（全栈源码核实）
- 任务：分析 HoYoMusic 全项目现状，用户明确要求"文档不可靠，永远信赖源码"。
- 方法：并行 4 个 Explore 子代理分别分析 backend/frontend/client-desktop/client-mobile，并亲自 grep 验证 DB schema 与代码脱节。
- 关键发现：
  - 架构：1 后端(Express5+PostgreSQL) + 3 客户端(Web React19/Vite、Desktop WinUI3/.NET8、Mobile Kotlin/Compose)，三端共享 REST 契约 `music.hoyodb.com/api`。
  - **最大风险：db/*.sql 仅 18 张表，后端代码引用 14 张缺 DDL 的表(favorites/playlists/app_settings/site_messages/feedback_messages/artist_aliases/canonical_credits 等)** → 仓库 SQL 无法重建生产库。
  - 零测试(后端 test=exit 1 占位)，仅移动端有 11 单测。
  - 前端缺 public/placeholder-cover.jpg(404)；5 个死组件未接入。
- 产出报告：`PROJECT_STATE_ANALYSIS.md`（仓库根目录）。

## 第二轮深挖（DB 不可重建 + 行动优先级）
- 新核实事实（grep + 读 setup.ts）：
  - `backend/src/setup.ts:11` 只读取并执行 `db/schema.sql`（**仅 10 张表**）；`db/init_db.sql`（16 张）在代码中**完全未被引用（孤儿文件）**。→ `npm run setup` 建出的库缺 ~22 张表，应用无法启动。仓库当前**完全无法复现可工作的数据库**（线上能跑是因为用了未入库的 schema）。这是项目最高危项。
  - `backend/src/index.ts:218` 已有 `GET /api/docs.json` 暴露完整 OpenAPI 3.0 规范 → 三端 codegen 替换手写模型层可行（防漂移杠杆最高）。
  - 后端无测试框架（devDeps 仅类型定义 + nodemon/ts-node/typescript；test=exit 1）。
  - git：main 分支，活跃；桌面端有大量**未提交** .xaml 改动（重构进行中）。
- 行动优先级已写入报告第 6 节：P0 数据库可重建 → P1 契约硬化(codegen+测试) → P2 架构减负(桌面端 Tauri 化) → P3 打磨。
- 用户关注点：①全项目现状 ②原生(桌面/移动)是否合适 ③现在该干什么。判断：移动原生合适(后台播放刚需)，桌面原生存疑(与 Web 重叠,建议 Tauri 套壳 React)。

## 用户补充的关键背景（22:09）
- 项目是**多手 AI 维护的屎山**；**当前线上服务正常运行**，其数据库被多轮 AI 加工过，是**唯一真值**。
- 仓库 `db/*.sql` 滞后、不可信，**禁止当作线上结构依据**；需要真实结构须以线上库 `pg_dump` 为准。
- 我上轮"应用无法启动"措辞需修正：那只针对**全新 clone + npm run setup**，不是线上服务。已把此约束写入项目 MEMORY.md。

## 22:11 用线上备份重建权威 schema（P0 完成）
- 用户提供线上库备份 `C:/Users/sumi/Downloads/hoyomusic_20260710221108aaaem.sql.gz`。
- 它是 **pg_dump 自定义二进制格式（PGDMP）**，非纯文本；用本机 `C:/Program Files/PostgreSQL/18/bin/pg_restore.exe` 的 `-s`(schema-only) 转纯文本。
- 真值比对结论（reconcile.py）：
  - 线上库 **27 张表**；代码引用的真实表与这 27 张**完全一致、一张不缺**。
  - **修正早先"14 张缺表"误判**：那些表在线上库本就存在（仓库 SQL 滞后而已）；其中 `canonical_credits/canonical_roles/canonical_stats/alias_matches` 是 `artistController.ts` 的 **CTE 别名**，不是表。
  - 仓库原有 `schema.sql`(10表) / `init_db.sql`(16表) 均滞后、且 init_db.sql 是孤儿。
- 已生成权威 `backend/db/schema.sql`（27 表，去掉了 psql 元命令/OWNER TO/GRANT，可被 setup.ts 执行），并 `git rm` 删除 `init_db.sql`。
- 关键约束已写入项目 MEMORY.md：未来任何表结构改动必须走迁移文件，否则会再次漂移；改动前先对线上库核对真值。
- 报告 PROJECT_STATE_ANALYSIS.md 相应订正（风险#1、结论、P0 标记完成）。

## 22:18 用户问"可否整理屎山"
- 我的建议：**可以，但禁止推倒重写**。多手 AI + 无测试 + 线上在跑 → 大改业务逻辑极易改坏。正确顺序：先 P1 安全网(迁移+契约测试+codegen)，再 B 表面清理(死代码/缺资源/重写不可靠文档)，最后 C 结构整理(桌面端 Tauri 减栈，可回退)。
- 核心认知：对屎山，"让它不再变糟"(安全网)比"收拾干净"价值更高。
- 本机已装 PostgreSQL 18 客户端；若也有服务端，可起本地测试库加载 schema.sql 真正跑漂移/契约测试，使清理可验证。等用户确认从哪一步入手。

## 22:24 全量执行（并行 5 subagent，已完成）
用户要求"开始全量执行 + 多开 subagent 并行"。派 5 个并行 agent（backend 安全网 / frontend 清理 / codegen / 文档 / 孤儿审计），各自锁目录互不冲突。**桌面端故意未动**（用户有未提交 xaml 重构在途）。
- **Backend 安全网(实测通过)**：`db/migrations/0001_init.sql`(=schema.sql 27表) + `scripts/migrate.ts`(轻量迁移器,`_migrations`表) + `package.json` 加 `migrate`/`test=vitest run` + `vitest.config.ts` + `tests/drift.test.ts`。agent 真起本地 Postgres 测试库加载 schema，**vitest 跑通 1 passed**；漂移报告 27 表全齐无缺。
- **Frontend 清理**：删 5 个未引用死组件(Library/SpectrumVisualizer/CrossfadeControl/KeyboardShortcutsModal/SleepTimer)+孤儿 CSS；生成 `public/placeholder-cover.jpg`；`trackService` 加内联 SVG 兜底消 404。
- **Codegen**：`openapi/openapi.json`(15 个已标注路由) + `frontend/src/generated/api-types.ts`(667行) + `api-client.ts`(类型化, tsc 过)。⚠️ 后端仅 15 路由有 swagger 注解，spec 是子集，需补注解才完整。
- **文档**：`docs/ARCHITECTURE_FROM_SOURCE.md`(~230行, 基于源码, 不确定处标 [UNVERIFIED])。
- **孤儿审计(read-only)**：`tools/` 5 文件均 orphan；删 2 个 dead Python 脚本(`git rm`)；2 个脚本因依赖缺失 SQL 待调查；README:415 引不存在的 start/stop-dev.ps1(死链)。
- 报告 PROJECT_STATE_ANALYSIS.md 已更新：P0/P1/B 标完成 + 产出清单。
- **结论**：项目从"脆弱多 AI 屎山"升级为"有迁移基线+测试护栏+准确文档"的可维护状态。剩余：P2 桌面端 Tauri 减栈(慎行)、P3 少量打磨、补 swagger 注解扩 codegen 覆盖。
